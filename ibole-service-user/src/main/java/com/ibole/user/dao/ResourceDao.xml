<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ibole.user.dao.ResourceDao" >


    <select id="findResourceByRoleIds" resultType="com.ibole.pojo.user.Resource">
        SELECT
            *
        FROM
            us_resource
        WHERE id IN (
            SELECT us_resource_id FROM us_role_resource WHERE us_role_id in
                <foreach collection="array" open="(" close=")" item="resId" separator=",">
                    #{resId}
                </foreach>
            )
    </select>
</mapper>