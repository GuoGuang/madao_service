server:
  port: 9003
spring:
  application:
    name: service-article #指定服务名

com:
  codeif:
    aopPointcutExpression: execution (* com.codeif.article..*.*(..))



hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: false
        isolation:
          strategy: SEMAPHORE