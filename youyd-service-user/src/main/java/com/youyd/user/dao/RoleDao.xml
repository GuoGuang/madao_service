<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.youyd.user.dao.RoleDao" >

    <!--更新角色是否禁用-->
    <update id="updateRole" parameterType="java.util.List">
        update role set state = #{state}
          where
          <foreach collection="list" item="roleId" separator="," open="id = (" close=")">
              #{roleId}
          </foreach>
    </update>
    <select id="findUsersOfRole" resultType="com.youyd.pojo.user.User">
        SELECT * FROM us_user WHERE id in (
            SELECT us_user_id FROM us_user_role WHERE us_role_id = #{roleId}
        )
    </select>
</mapper>