<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.youyd.user.dao.RoleResourceDao" >
    <insert id="insertBatch" >
        INSERT INTO us_role_resource(`id`, `us_role_id`, `us_resource_id`)
        VALUES
        <foreach collection="list" item="item" index="index" separator=",">
            (
            #{item.id},
            #{item.usRoleId},
            #{item.usResourceId}
            )
        </foreach>
    </insert>


    <select id="findResourcesOfRole" resultType="com.youyd.pojo.user.Resource">
        SELECT * FROM us_resource WHERE id in (
            SELECT us_resource_id FROM us_role_resource WHERE us_role_id in (
                SELECT id FROM us_role WHERE id = #{roleId}
            )
        )
    </select>
</mapper>